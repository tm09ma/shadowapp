// Lokale Entwicklung: Backend laeuft lokal auf 8080.
// authToken muss zum lokal gesetzten APP_TOKEN des Backends passen.
export const environment = {
  apiUrl: 'http://localhost:8080',
  authToken: 'PLACEHOLDER_TOKEN'
};
